import Signin from "@/components/pages/auth/Signin";
import { MainWrapper } from "@/styles/wrappers";

const Auth = () => {
  return (
    <MainWrapper>
      <Signin />
    </MainWrapper>
  );
};

export default Auth;
